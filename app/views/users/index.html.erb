<% @users.each do |f| %>
  <div class="each-user-div">
  <img src="<%= f.photo %> "  />

     <div>
        <p>   UserName: <%= f.name %></p>
         <p>  Number of Posts: <%= f.postscounter %></p>
    </div>
  </div>


<% end %>
